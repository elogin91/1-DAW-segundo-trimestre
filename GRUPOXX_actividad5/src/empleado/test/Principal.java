package empleado.test;

import java.util.ArrayList;

import empleado.javabean.Empleado;

public class Principal {
	public static void main(String[] args) {
		/*
		 * alta (empleado); eliminarUno (empleado); modificarUno (empleado);
		 * buscarPorSexo (empleado); buscarUno (idEmpleado); buscarTodos ();
		 * masaSalarial(); buscarPorDepartamento (idDepar); buscarPorTrabajo
		 * (idTrabajo); buscarPorPais (pais);
		 */
	}
}
